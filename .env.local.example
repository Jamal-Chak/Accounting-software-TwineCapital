# Supabase Configuration
# Get these from https://supabase.com → Your Project → Settings → API

# Replace with your actual Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Replace with your actual anon/public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Stitch Money Configuration (for bank integration)
# Leave empty until you get credentials from Stitch
STITCH_CLIENT_ID=
STITCH_CLIENT_SECRET=
STITCH_REDIRECT_URI=http://localhost:3000/api/banking/connect/callback
STITCH_WEBHOOK_SECRET=

# Encryption Key for storing bank tokens securely
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
